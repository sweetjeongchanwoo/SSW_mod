reintegrate_states = {
	allowed = {
		OR = {
			tag = RUS
			tag = SOV
		}
	}
	visible = {
		OR = {
			tag = RUS
			tag = SOV
		}
	}
}
interventio_in_german_civil_war = {
	allowed = { tag = RUS }
	visible = {
		has_completed_focus = RUS_interventio_in_german_civil_war
		tag = RUS
		OR = {
			GER = { has_war_with = LTV }
			GER = { has_war_with = HLH }
			GER = { has_war_with = HGR }
			LTV = { has_war_with = HLH }
			LTV = { has_war_with = HGR }
			HGR = { has_war_with = HLH }
		}
	}
}
