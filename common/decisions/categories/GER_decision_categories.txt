GER_civill_war_militia = {

    icon = military_operation
	
    allowed = {
	    original_tag = GER
    }

    visible = {
        has_war_with = HGR
        has_war_with = HLH
        has_war_with = LTV
    }
}

GER_is_debug = {

    icon = military_operation
	
    allowed = {
	    original_tag = GER
    }

    visible = {
        is_debug = yes
    }
}